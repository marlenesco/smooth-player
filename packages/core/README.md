# @smooth-player/core

Workspace placeholder for the future core package extraction.

Current status:
- The production core code still lives at repository root (`src`, `styles`, `assets`).
- This workspace is intentionally scaffolded first to prepare a safe migration path.

Next migration step:
- Move core source/build config from root into this package.
- Keep `smooth-player` as the published package name.
